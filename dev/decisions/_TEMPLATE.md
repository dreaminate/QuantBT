<!-- 【开发os级别·模板】勿改本文件。开新机/新身份:按下面二选一建到 decisions/{developer_id}/,删本注释。
     两种形态皆合法(RULES §4),选一种坚持用,别混:
       A. 单文件账本  decisions/{id}/DECISIONS.md —— append-only,新决策追加在末尾,永不改既往条目。
       B. 一决策一文件 decisions/{id}/<锚>-<slug>.md —— 一个决策一个文件,文件本身 append-only。
     锚语法(硬约定,引用全靠它):`D-####`(账本内条目)或 `ADR-<NS>-###`(单文件名,NS=项目命名空间大写)。
     GOAL/TRACE/任务卡引用决策**只锚这两种格式**;canonical/全局决策归 leader 的 folder。 -->
# DECISIONS · <developer_id>（append-only · 锁定不改既往）

> 一条决策 = 锚 + 拍板内容 + 依据 + 日期。**已决不重议**;要推翻就新开一条并注明「取代 <旧锚>」。

## D-0001 · <一句话:决了什么>
- **日期**:YYYY-MM-DD · **拍板人**:<operator/leader id>
- **决定**:<选了什么>
- **依据 / 代价**:<为什么;放弃了什么>
- **影响面**:<GOAL §x / 卡 uuid8 / 文件>

<!-- 批量拍板日(一天拍多条)也用本骨架:同一文件里连续多条 D-####,每条独立锚——
     禁止把多个决定挤进一个锚(锚是引用的最小单位)。 -->
