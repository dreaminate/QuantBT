<!-- 【开发os级别·模板】勿改本文件。需要跨会话续接时:复制到 state/{developer_id}/frontier.md,删本行再写。
     frontier = 前沿快照,**重生型**:每个 loop/session 收尾**整篇覆写**,永远只有最新一份(历史在 git)。
     它专门收留「上次刷新」「本会话续接」这类块——**这类内容禁止写进 state.md**(validate_dev 会抓堆叠)。
     允许长(全量上下文快照就是它的职责),但只此一份、不堆叠。 -->
# FRONTIER · <developer_id> 前沿快照（重生型 · 每次整篇覆写）

> 给下一个 session/loop 的续接现场。**覆写,不追加**——旧快照不保留(要历史看 git log 本文件)。
> 与 `state.md` 分工:state = 对照 GOAL 的 gap 表(慢变);frontier = 正在进行的战役现场(每 loop 全刷)。

## 现在打到哪了
<当前波次/任务的全量现场:在做什么、做到哪一步、下一步第一个动作是什么。>

## 活跃上下文
<下个 session 不读就会重犯的关键事实:关键文件/分支/命令、刚踩的坑、临时约定。>

## 待裁 / 卡点
<等拍板的事项(逐条)、被阻塞在哪。没有就写「无」。>
