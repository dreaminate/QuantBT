<!-- 重生型:每个 loop/session 收尾整篇覆写,永远只有最新一份(历史在 git)。模板 state/_TEMPLATE.frontier.md -->
# FRONTIER · dreaminate 前沿快照（重生型 · 每次整篇覆写）

> 给下一个 session/loop 的续接现场。**覆写,不追加**——旧快照不保留(要历史看 git log 本文件)。
> 与 `state.md` 分工:state = 对照 GOAL 的 gap 表(慢变) + 顶部刷新块(本轮值);frontier = 正在进行的战役现场(每 loop 全刷)。

## 现在打到哪了
- **/loop 5m 自主循环运行中**（cron 本 session 内·prompt 含「效率优先·多 agent 并行·放权」）。
- **【最新·2026-07-16】M6b 已 land main（产品 `a8549fa8`）= 内嵌 agent epic (M1→M6b) 整条闭环收官。**
  - 本轮 loop 唤醒先做 **recovery**：本地 main 曾落后 origin/main 86 commit（皆前序已 land 工作）→ `git merge --ff-only` 到 `7aa19eed`（HEAD 为 origin 祖先双证·干净 FF·GOAL 零 diff）。recovery checkpoint = `dd5c1800`（dev/ state·随 M6b 一起 push）。
  - **M6b 内容**（app/agent/backends + main.py + 4 新测·扩展不替换）：SSE liveness 守卫——A1 idle+total timeout（reader 线程+bounded queue·抗慢消费者背压）、A2 orchestrator 显式 close backend.run 确定性 cancel、A2.5 EOF-but-alive bounded `proc.wait(eof_grace)`、A2.6 EOF 无 terminal→honest BackendError（非假 stream_ended）、A3 `_agent_workspace_for_owner` --add-dir 路径穿越守、B4 opt-in 真 claude 冒烟经 SSE orchestrator（CI 诚实 KNOWN_RUN_GAP skip）+ `SessionStarted.mcp_servers`。
  - **跨厂商 codex floor 5 轮**（approver≠creator·GPT-5.6-sol）逮 **3 真 correctness/honesty 洞**（EOF sentinel 满队列丢弃 · A2 双 regression〔eager-run 抛错逃逸+close 异常泄漏〕· EOF-no-terminal 假绿）+ 多处 honesty 过强描述 → 逐条修 + RED-then-revert 钉 → R5 **FLOOR-HOLD**。诚实教训：cancel 重构一移就悄悄收窄了 honest-error 保证；同厂商复读易放过，跨厂商逮住。
  - **诚实 residual（登记·非阻塞·pre-existing）**：HTTP 断连若 ASGI server 不 close 生成器 → 显式 close 不触发、idle/total 也不推进（消费者停拉冻结 deadline 检查）→ child 靠 GC 回收、无全局 CPU/进程上界、L-C/L-D 仍守、非安全边界。→ **route-level `request.is_disconnected` 硬化 = follow-up**。

## 下一步（NEXT · 最高杠杆）
- **金融数学 kernel P0**（北极星① 数学贯穿·最大缺口）：agent epic 闭环后，转「数据→因子→模型→信号→组合→执行→回测→归因→监控」链上机构级数学的**理论先证明 → 一致性校验门**主干。分解草案见 `research/findings/dreaminate/`（金融数学 P0-P3）。实质性切片走 duet（deep-opus ‖ codex），跨厂商 floor 复审。
  - **✅ P0-A #1 VaR/ES+Kupiec+spine 命门已 land**（产品 d8169028·2026-07-16·codex 17 轮 floor FLOOR-HOLD·CI 29544874836 success·见 [[decision D-MATH-P0A-VARES]]）。
  - **🔨 IN-PROGRESS=P0-A #8 组合 correctness（真 ERC）——solver 已实现+数值实证，命门/测试/floor 待续（WIP·optimizers.py 工作区未提交）**：
    - **✅ 三方 duet 设计闭环**（deep-opus ‖ codex/GPT-5.6-sol ‖ 我·codex=授权数学决策者 D-MATH-DECIDER 裁分歧）。**codex 裁决**（存 scratchpad `erc_consolidated_design.md`）：solver=**相关空间 R=D⁻¹ΣD⁻¹ 凸对数障碍 damped Newton**（Cholesky 解方向·Armijo 保正线搜·非 CCD 非 SLSQP）；oracle=**dense-RC 残差**（impl 返回 N·r̂_i、oracle 返回全 1·另路重算·主）+ **对角/2-asset 闭式**（vs inverse_volatility·次）；**inverse_volatility 不进 ERC 实现链指纹**（它=对角 oracle·须两条独立路径·codex 推翻 deep-opus）；**apply_constraints blocker**（optimize_portfolio 后置约束截断破 ERC）→ **dispatch 接线 defer 到 follow-on**（本切只 raw `equal_risk_contribution`+命门）。
    - **✅ 已实现+实测**（optimizers.py·**工作区未提交**·未 land）：诚实改名 `risk_parity→inverse_volatility`+保 `risk_parity` alias（test_portfolio 9 + test_academic_audit_v2 28 全绿·非破坏）；`equal_risk_contribution(cov,*,rc_tolerance=1e-8,max_iterations=100)`+`ERCError`+私有 `_cov_to_correlation_strict`/`_erc_newton_solve`/`_erc_normalize_logdomain`（fail-closed·非 SPD/近秩亏/不收敛/下溢即 raise·不静默钳/jitter/shrink/等权兜底）。**数值探针 7/7 过**：codex 3-asset 例 ERC RC=(⅓,⅓,⅓)@E_RC=5e-15、inverse-vol RC=(3/7,3/14,5/14)【跨厂商吻合 codex 独立探针】；对角/N=2 退化=inverse-vol（machine ε）；置换等变/正尺度不变；奇异/非对称 raise。
    - **✅ 命门绑定 + 对抗测已实现+过（工作区未提交）**：`app/backend/app/portfolio/spine_binding.py`（ERC_ARTIFACT[ARTIFACT_ESTIMATOR,PROOF_BACKED]+ERC_DATA_CONTRACT+_ERC_IMPL_CHAIN[仅 solver 4 环·不含 inverse_volatility/oracle]+dense-RC 残差 oracle[主]+对角/2-asset 闭式 oracle[次]+`ERC_PINNED_FINGERPRINT="84aaf8f091414cb7"`+verify_erc_consistency）+ `tests/test_portfolio_erc_spine.py`（**19 测全过**）+ `__init__.py` 导出 inverse_volatility/equal_risk_contribution/ERCError。**命门实证**：true ERC→proof_backed；**MUTATION inverse_vol 当 ERC impl→dense-RC fail(dev=0.406@极端vol fixture)→challenged**（种坏门必抓✓）；staleness current≠pinned→exploratory。solver 修：加 `max_iterations<1` 守卫 + `e_rc=inf` 初值（补 0-迭代 NameError 洞）→重 pin。**快门全绿**：compile OK·diff-check clean·validate_dev PASS·audit 基线 61/26209663/7348e826 不变。
    - **✅ codex floor R1 = FLOOR-HOLE 6 洞·已全修+实证**（全量 R1 已绿 7038·但 floor 逮 6 真洞·未 land）：① **命门漏 long-only**（dense-RC 只核 RC=1/N·SPD 签名象限解 {1.45,-0.86,0.40} RC 误差 8.9e-16 也过→proof_backed）→ dense 诊断加 positivity_flag（键集精确∧全有限∧全>0）+ 签名 mutation 测·实证 signed→challenged ② **equal_risk 公开 API 交 inverse-vol**（business_tools:578）→ 接真 equal_risk_contribution·不施 post-hoc cap（破 RC）·诚实 note + schema 描述 + 2 入口测 ③ **N=1 快捷/非方阵/复数 绕过校验**（[[0]]/[[nan]]/[[inf]]/1×0/complex 都放行）→ 校验前移到 n==1 前·复数拒·只真 0×0 返 {} ④ **对称容差用全局绝对尺度**（强异方差小子块反对称漏）→ 相对相关尺度 |Σij−Σji|/√(ΣiiΣjj)≤1e-10 ⑤ **scale-safe 对极端合法 SPD 失效**（diag 1e308 溢出/subnormal 下溢误拒）→ 先缩放再对称化·后置 RC 用缩放协方差 ⑥ **指纹漏模块级 import**（重绑 cho_solve/改 import 源不触发 staleness）→ solver 拆独立 **`_erc_solver.py`**·指纹整模块文本。**重 pin `ERC_PINNED_FINGERPRINT="febdc5e04cfc3b32"`**。ERC spine **25 测**（19+6 新对抗）+ business_tools 2 入口测 + 基线 37 全绿·快门全绿（compile/diff-check/validate_dev/audit 61/26209663/7348e826 不变）。数学主体 R1 已判 sound（FOC/Hessian/映回/E_RC 等价/rank guard·100 SPD 探针 worst 9.14e-9）。
    - **✅ codex floor R2 = FLOOR-HOLE 7 洞·已全修+实证**（全量 R2 已绿 7046·floor R2 又逮 7 洞，含 1 个我 R1 修引入的 P0 新 bug）：① **[P0] scale-first 假 ERC**（我 R1 hole5「先除全局 max 方差」在强异方差 diag 跨 1e308 下把小 off-diag 下溢成零→solver 悄解对角化错问题→假 E_RC=0.0625·有损后置看不到）→ **改逐元素 R_ij=Σ_ij/(σ_iσ_j)**（每项按自身尺度·小相关不丢）+ 后置 RC 在相关空间无损·实证真 E_RC=4.2e-14 ② **[P1] equal_risk 用 synthetic cov** → 披露 `covariance_source=synthetic_demo`（N≥60 raise 诚实）③ **[P1] risk_limits 谎报 max_position**（equal_risk 不 cap 却回 max_position=0.05）→ 报 `requested_max_position + max_position_enforced=false` ④ **[P1] 指纹漏 re-export 桥**（重绑 optimizers.equal_risk_contribution 不触 staleness）→ 指纹加 `bridge_intact` 身份校验 + ref 指 _erc_solver.py ⑤ **[P1] Armijo 近解停滞**（2/100 良态 SPD 卡 ~6.9e-8）→ 加降-E_RC 回退步 + 停滞检测（返达到值交后置门·200 探针 0 失败）⑥ **[P2] 未知 sizing 静默退等权** → handler 白名单 fail-closed ⑦ **[P2] 签名 mutation 测 fake-green**（旧向量非 ERC·被 RC 门逮非 positivity）→ 换真签名 ERC（w_i=s_i/σ_i·RC=1/N 精确）真隔离 positivity 门。**重 pin `c45efb65c46c6f1a`**。ERC 25 测 + business_tools 2 入口测 + 基线 74 全绿·快门全绿·audit 61/26209663/7348e826 不变。
    - **✅ codex floor R3 = FLOOR-HOLE 4 洞·已全修+实证**（全量 R3 已绿 7046·floor R3 又逮 4 洞·全 P1/P2·数学主体三轮判 sound）：① **[P1] 停滞检测误拒可解 SPD**（我 R2 停滞短路太急·早期 Armijo 非单调把大残差 E_RC=10 矩阵当地板过早返回→后置 raise·实证该矩阵 14 迭代能收 1.6e-9）→ **停滞短路仅在 E_RC≤硬地板 `_HARD_RC_FLOOR=1e-6` 才返回**·否则续迭到预算（真不收敛才 raise）② **[P2] falsey sizing 静默退等权**（`or` 让 ""/None/0/[]/{} 也退等权）→ `args.get("sizing","equal_weight")` 仅缺 key 补·余过白名单 ③ **[P2] 非法 max_pos 静默换 0.03 却标 requested**→ 显式非数值/非有限/越界 fail-closed error·dd_halt 同 ④ **[P2] 回归测 fake-green**（旧均匀大/小矩阵测·buggy 全局尺度也过）→ 加**混合尺度 diag(1e308,2e-15) P0 真复现**+ill-conditioned 收敛+bridge-rebinding+business_tools disclosure/enforcement/falsey/invalid 回归。**重 pin `549e401f26c10799`**。ERC 28 测 + business_tools 4 入口测 + 基线 76 全绿·快门全绿·audit 不变。混合尺度真 E_RC=4.2e-14·200 探针 0 失败。
    - **✅ codex floor R4 = FLOOR-HOLE 4 洞·已全修+实证**（全量 R4 已绿 7051·数学主体四轮判 sound·codex 确认停滞守卫修了真复现例）：① **[P1] 后置校验了中间 y 而非返回 w**（solver 的 y-based e_rc≤1e-6 但 log 域 softmax 归一有舍入·返回 w 的 E_RC 可达 8.09e-6 超地板）→ **后置改校验返回权重 w 本身**（u=σ⊙w∝y·尺度消去 rc_i=u_i(Ru)_i/(uᵀRu)·无损）② **[P2] 硬地板悄改 rc_tolerance 契约**（rc_tolerance=1e-12 返 2.5e-8·0/负/NaN 也返）→ 校验 rc_tolerance 有限正·docstring 明契约（rc_tolerance=目标·1e-6=硬保证）③ **[P2] ill-conditioned 测 fake-green**（旧无守卫逻辑也过）→ 换**真 premature-stall 复现**（旧 iter3 E_RC=30 就返回·当前收 1.2e-10·旧逻辑过不了）④ **[P2] 显式 None 仍走缺省 + bool 当数值**（max_pos=None→0.03·max_pos=True→1.0）→ 仅 key 缺失才补·拒 None/bool。**重 pin `66b031adef404f5e`**。ERC 29 测 + business_tools 4 入口测 + 基线 76 全绿·快门全绿·audit 不变。
    - **✅ codex floor R5 = FLOOR-HOLE 3 洞·全 P2·全同一 rc_tolerance 契约主题·已全修**（全量 R5 已绿 7052·codex 确认 4 个 R4 修全 genuine + 数学主体五轮 sound）：① loose rc_tolerance（1.0）让 solver 早停 E_RC=0.027 而后置门误拒可解矩阵 → 求解目标夹 `min(rc_tolerance, 硬地板)` ② artifact failure_conditions 仍说「未达 rc_tolerance→raise」（假·实为达硬地板）→ 改「rc_tolerance=best-effort·硬保证 E_RC≤1e-6」 ③ rc_tolerance=True 绕过校验（bool⊂int）→ 显式拒 bool。**重 pin `a1a3be1aa881018f`**。ERC 30 测 + business_tools 4 入口测 + 基线 76 全绿·快门全绿·audit 不变。
    - **✅ codex floor R6 = FLOOR-HOLE 1 洞·P2·已修**（全量 R6 绿·codex 确认 3 个 R5 修全 genuine·数学主体六轮 sound）：紧 rc_tolerance+小 max_iterations 时预算耗尽/线搜停滞路径**仍 raise 尽管 E_RC 已达硬地板**（实证 rc_tolerance=1e-8,max_iter=3→E_RC=6.69e-8≤1e-6 却 raise·违 best-effort 契约）→ **两条终止路径都改「已达硬地板即 return·否则 raise」**·使收敛/停滞/线搜停滞/预算耗尽四路径对硬地板同一裁定。实测 max_iter=1/2(E_RC>1e-6)raise·max_iter=3(6.69e-8)return·max_iter=0 guard raise。**重 pin `0840b622b19910a7`**。ERC 31 测 + 基线 76 全绿·快门全绿·audit 不变。
    - **✅ LANDED main（产品 `bc1ff918` + dev/ `e43a86a2`·2026-07-16）**：codex floor **7 轮**（R1-R6 共 25 洞逐修·含我 R1 修引入的 P0 scale-first 假 ERC→R7 FLOOR-HOLD·数学主体七轮 sound）。四门齐：全量 v8 clean **7054 passed/0 failed**（M6b flake 满载偶发·低载 v8 全绿·孤立 3/3 过·pre-existing 无关·task_6c7609a7）+ validate_dev PASS + floor R7 FLOOR-HOLD + audit 基线不变。详见 state 顶部刷新块 + [[decision D-MATH-P0A-ERC]]。全 floor 记录 `scratchpad/erc_floor{,2..7}_out.md`。**CI=Unqueried（下轮 gh 查 bc1ff918 的 run）**。
    - **🔨 IN-PROGRESS=组合 correctness 续切「真 LedoitWolf」（三方 duet 闭环·solver 已实现+实测·WIP 未提交）**：
      - **✅ 三方 duet**（deep-opus ‖ codex/GPT-5.6-sol ‖ 我·codex=授权数学决策者）：真 LW-2004 公开口径 **δ*=min(1,π̂/(Tγ̂))**（classic/sklearn·**不塞 ρ̂-corrected**——后者 δ 差 ~0.047 非舍入·是另一 artifact·codex 裁）·Σ*=(1−δ*)S+δ*μI·S=XᵀX/T MLE（非 ddof=1）。impl=**sklearn.covariance.LedoitWolf**（BSD 已装·prior-art）·命门 oracle=手写 outer-product 路径（另一条独立代码路径）。两 thinker 各实证手写 δ*==sklearn 到 1e-16 + dry-run 过真 gate（real→proof_backed·mutation→challenged）。设计全文 `scratchpad/lw_duet_codex_out.md`+deep-opus task。
      - **✅ 已实现+实测**（`_lw_shrinkage.py` 新·**WIP 未提交**）：`ledoit_wolf(returns)→LedoitWolfResult(cov,δ,location)`（sklearn·scale-safe·fail-closed）+ 诚实 `constant_shrinkage(cov,*,alpha)`（原固定-α）+ `ledoit_wolf_shrinkage` DeprecationWarning shim（出 __all__·扩展不替换）+ `CovarianceEstimationError`。**探针实证**：impl δ*==手写 oracle δ* 到 **1e-17**（3 regime：correlated/high-shrink T<N/low-shrink·全 SPD）·MUTATION 固定-α 0.2 vs 真 δ 差 0.14·fail-closed（T<2/复数/非有限/1D/μ=0/α 越界）全 raise·N=1 δ=0。
      - **✅ 实现全完+本地实测**（5 文件·WIP 未提交）：① `hrp_audit.py`（M·删 def·从 _lw_shrinkage re-export·`optimize_hrp_safe` 用真 `ledoit_wolf(returns)`·HRPResult.lw_shrinkage 字段·warning 真 δ·__all__ 去 shim·实证近奇异 returns→δ*=0.0574 数据驱动）② `test_academic_audit_v2`（M·→constant_shrinkage+pytest.warns·**28 passed**）③ **`hrp_shrinkage_binding.py`（新·命门）**：LW_ARTIFACT[ARTIFACT_ESTIMATOR·PIT]+独立手写 LW-2004 outer-product oracle+MUTATION+指纹〔code_fingerprint 链+`sklearn.__version__`+numpy+public_api+bridge_intact·不 hash sklearn 源〕+`LW_PINNED_FINGERPRINT=4a27529cbada7b7a`·**实证 real→proof_backed·固定-α 0.2/0.3 mutation→challenged(dev 0.259)·staleness→exploratory**④ `tests/test_portfolio_lw_spine.py`（新·**12 passed**：impl==手写 oracle 1e-12·δ* 随 regime 变·SPD 含 T<N·尺度不变·shim deprecated·fail-closed·MUTATION）。快门全绿（compile/diff-check/validate_dev/audit 不变）·affected 基线 80 passed。
      - **✅ codex floor R1 = FLOOR-HOLE 4 洞·已全修+实证**（floor-1 out `scratchpad/lw_floor2_out.md`·approver≠creator·GPT-5.6-sol）：① **[P1] δ*=0 时返回秩亏非 SPD 协方差**（`if δ*>0` 才查 Cholesky·T=2 对映/特定 T<N → LW δ*=0 却 S 奇异 → 特征值含负也放行·同 scale² 下溢藏漏）→ **无条件 Cholesky** + scale²>0∧有限 guard + cov shape 校验（`_lw_shrinkage.py`）② **[P1] optimize_hrp_safe 把非法 returns（NaN/T=1/全零）伪装成 risk-parity 权重**→ 入口 `_validate_returns` prevalidation（守单资产分支+fallback 关路）+ **删 `except CovarianceEstimationError→risk_parity` 吞**·退化数据 raise 传播（区分 input-contract 违反 vs 合法数据估计器退化·`hrp_audit.py`）③ **[P2] 指纹漏模块级 solver 重绑**（`_lw.LedoitWolf=OAS` δ 变 0.052→0.113 而指纹不变·`code_fingerprint` 只哈希函数体）→ **whole-module 源**（镜 ERC）+ 运行时 `solver_binding_intact` 身份双查（`hrp_shrinkage_binding.py`）④ **[P2] artifact 夸大「T<N→δ*→1」**（实为 S 奇异·δ* 幅度数据依赖·本切 T=20,N=40 δ*=0.089）→ 改诚实表述。**重 pin `LW_PINNED_FINGERPRINT=4777cacb2f120654`**。**種坏门必抓 RED-then-revert 3 bug 全过**（重现 δ*>0 guard→δ*=0 测 RED·删 prevalidation→短路路测 RED·删 solver_binding_intact→重绑测 RED·文件复原 byte-identical）。LW spine **19 测**（12+7 新）+ academic 28 + portfolio 9 + business 39 + ERC spine 31 = **126 affected 全绿**·快门全绿（compile/validate_dev/diff-check/GOAL 零 diff）·audit 基线不变（7348e826）。**数学核未动**：3 fixture δ* 恒等（0.0517/0.0891/0.000403）·impl==oracle 1e-17。
      - **✅ codex floor R2 = FLOOR-HOLE 4 洞·已全修+实证**（floor-2 out `scratchpad/lw_floor3_out.md`·codex 确认 R1 洞 3/4 全闭、洞 1/2 核对·又逮 4 极端边缘洞）：① **[P1] 对称化溢出成 inf**（`0.5*(cov+cov.T)` 中间 2e308 溢出·有限性检查在对称化前·`[[-1e154],[1e154]]`→Σ*=inf·Cholesky([[inf]]) 不抛）→ **overflow-safe `0.5*cov+0.5*cov.T`** + 有限性检查移到对称化后 + Cholesky 因子有限查（返回有限 [1e308]·1e160 raise）② **[P1] 全零修不彻底**（`_validate_returns` 不查零中心尺度 → 单资产 zeros→{A:1.0}·fallback 关+zeros→risk_parity）→ 入口 `cscale=max|xc|` 门（>0∧有限∧cscale² 有限）拦在单资产/fallback 两短路前 ③ **[P1] 非有限协方差当健康**（`is_near_singular([[inf]])→(False,…)`·极端有限 returns→np.cov=inf→HRP 出 {A:0.5,B:0.5}）→ is_near_singular 非有限 fail-closed + np.cov 后有限性 raise + except 改 raise（不吞等权）④ **[P2] 重复 symbol 权重不和 1**（`["A","A"]`→dict 塌缩→{A:0.508}）→ `len(set)!=n` raise。**+删死码 `_equal_weights`**（本轮 np.cov except 修后孤儿）。**重 pin `LW_PINNED_FINGERPRINT=42319cf8e59d4db2`**。**種坏门必抓 RED-then-revert 4 bug 全过**（复原 byte-identical）。LW spine **23 测**（19+4）+ academic 28 + portfolio 9 + business 39 + ERC spine 31 = **130 affected 全绿**·快门全绿·audit 不变·**3 fixture δ* 恒等**（0.0517/0.0891/0.000403·impl==oracle 2.8e-17·数学核三轮未动）。
      - **✅ codex floor R3 = FLOOR-HOLE 5 洞·已全修+实证**（floor-3 out `scratchpad/lw_floor4_out.md`·codex 确认 R2 4 修全闭·又逮 5 洞·**根因浮出=HRP 非全局尺度不变**）：① **[P1] `scale²` 预门误拒可表示大尺度**（1.35e154→scale²=inf 但 Σ*≈1.03e308 可表示·δ*=1）→ 删 sq 门·逐元素 `(cov_dim*scale)*scale`（交错 cov_dim≲1 避中间溢）+ errstate 抑噪·真不可表示由对称化后有限性+Cholesky 因子兜底 raise ② **[P1] optimize_hrp_safe 非全局尺度不变**（大尺度 np.cov→inf 误 raise·小尺度 vol 触 1e-12 地板→伪等权 {0.5,0.5}）→ **根修：归一 xn=xc/cscale·全部（np.cov/奇异/LW/HRP）在无量纲空间算·权重与 raw 恒等**（实测 ×1e154/×1e-160 权重 maxdev 1.67e-16）③ **[P2] symbol 身份不全**（None/""/纯空白/不可哈希 放行或 raw TypeError）→ set 前逐个 `isinstance str ∧ strip()` ④ **[P2] fallback 仍错标 risk_parity + 文档留删除的 rung**（对角反波动率≠真 ERC·同 ERC 改名一类）→ `_risk_parity_weights→_inverse_volatility_weights`·`fallback_used`/Literal/warning/docstring/academic 测全改 inverse_volatility ⑤ **[P2] 2 回归测欠力**（corr=1 放宽三选一·1e200 测在更早门 raise 没测到目标 seam）→ corr=1 精确断言 hrp_shrunk+正 δ·非有限 guard 直测 is_near_singular seam。**重 pin `LW_PINNED_FINGERPRINT=9102d88d878b7fb3`**。**種坏门必抓 RED-then-revert 4 code bug 全过**（含修正 test fixture 后 R3-1 真 RED·复原 byte-identical）。LW spine **27 测**+ academic 28 + portfolio 9 + business 39 + ERC spine 31 = **134 affected 全绿**·快门全绿·audit 不变·**3 fixture δ* 恒等**（数学核四轮未动）。
      - **✅ codex floor R4 = FLOOR-HOLE 5 洞·已全修+实证**（floor-4 out `scratchpad/lw_floor5_out.md`·codex 确认 R3 5 修全 genuine·200-例 fuzz 0 假拒·又逮 5 归一 refactor 数值边缘洞）：① **[P1] 中心化 `r.mean` 大均值列 sum 溢出 inf 误 raise**→ scale-safe `(r-r[0])-mean(r-r[0])`≡r-mean(r)·不 sum 大常数（实测常数列 0→1e308 平移 δ* 恒等·旧 raise）② **[P1] 全局归一冲掉超动态范围列**（非常数列比值≲1e-12 → vol 压地板下 → 伪权重）→ per-column `col_vol_n≤1e-12` fail-closed（全动态范围保真 HRP 登记 follow-on）③ **[P2] min_eigval 静默换无量纲单位**→ HRPResult 加 `normalization_scale=cscale`·doc 明 min_eigval 无量纲/cond 尺度不变 ④ **[P2] symbol 首尾空白别名**（"A" vs "A "）→ `s!=s.strip()` 拦 ⑤ **[P3] 陈旧 test/doc**（多资产 equal_weight rung / risk-parity 文案）→ 清。**重 pin `LW_PINNED_FINGERPRINT=a0e2c805b1e055fd`**。**種坏门必抓 RED-then-revert 4 code bug 全过**（复原 byte-identical）。LW spine **31 测**+ academic 28 + portfolio 9 + business 39 + ERC spine 31 = **138 affected 全绿**·全量后端 **7080 passed/1 failed**（=M6b pre-existing flake·孤立过·非 LW）·快门全绿·audit 不变·**3 fixture δ* 恒等**（数学核五轮未动）。
      - **🔜 待续（next fire·验证在飞）**：post-R4-fix 全量后端套件 **bg id bgb18xgs6**（log `scratchpad/lw_fullsuite6.log`·期 7080±·M6b flake pre-existing）+ 跨厂商 codex **floor R5 bg id brvtz4lnj**（prompt `scratchpad/lw_floor6_codex.md`·out `scratchpad/lw_floor6_out.md`·验 R4 5 修 genuine + hunt 新洞·或 FLOOR-HOLD 收口）。next fire：查两 log → 全量绿（modulo M6b flake）+ **R5 FLOOR-HOLD** → 四门齐 → **整切一次 land**（product 单 commit·5 文件·pin a0e2c805）+ decisions 落 D-MATH-P0A-LW + push；R5 FLOOR-HOLE → 逐修再验再 floor（ERC 纪律·5 轮收敛中·R1 结构→R2 极端边缘→R3 尺度不变根因→R4 数值边缘→R5 或收口·类比 ERC 7 轮·数学核始终未动）。设计全文 `scratchpad/lw_duet_codex_out.md`。
    - **⚠️ gate③ CI-reliability（pre-existing·非我切引入·非 land 阻塞·实锤 3 次）**：`986ff288`（ERC land 含产品码）CI=**failure**·失败 4 测**全在 chunk (4)**·**全无关 ERC·全本地 v8 7054/0 过**（ff2ccb3e/1192cba2/986ff288 皆 chunk-4·不同测同 chunk·8f642b57 全绿）=**chunk-4 重集成测（onboarding/llm/trust 重 TestClient）资源耗尽 flake**。修候选：pytest-rerunfailures 重试 / 重平衡 chunk / 增资源。correctness 优先·此为 gate③ infra 候选·登记待选。
    - **⚠️ 登记 gate③ CI-reliability（pre-existing·非 ERC 引入·非 land 阻塞）**：CI `.github/workflows/ci.yml` 后端 5-chunk 并行（matrix 0-4·独立 runner），dev-only commit（ff2ccb3e/1192cba2）偶发多个**无关**测失败（artifact_trust_gate/llm_use_binding/monitor_production/portfolio_gate·跨 chunk·本地 v8 全过·8f642b57 又全绿）=**并行资源竞争 flake**。候选修：pytest-rerunfailures（known-flaky 重试）或降 chunk 并行或增 runner 资源。ERC land 的四门是**本地全量 v8 7054/0**（非 CI）·CI 是 gate③ 报告项据实填。
    - **登记 follow-on**（非本切）：(a) dispatch 接线 "erc" 进 optimize_portfolio + 非绑定约束守卫（约束绑定即 raise "constrained ERC not implemented"）(b) 真 sklearn LedoitWolf（hrp_audit.py:61 固定 α→自动·签名 cov→returns·ripple hrp_audit:134+test_academic_audit_v2:287）。
    - **待用户拍（方法学·登记不停摆·Inference 默认可翻案）**：改 "risk_parity" 串行为(inverse-vol→真ERC)会变 agent 工具语义→保 alias+加新选项(不改旧串)；ARTIFACT_ESTIMATOR vs PORTFOLIO_OBJECTIVE(选 ESTIMATOR·触 PIT 零门改)；riskfolio dev-only 非运行时。
  - **P0-A 其余候选**（seam §86 top-6·三方一致）：EVT-POT 尾部引擎(#6/#7·additive·扩 VaR/ES·scipy.genpareto)、measure/process typed 层(#1/#2·架构地基)。
- 其余仍开本地 gap 与待拍板项见 **state 顶部刷新块「仍开·待拍板/外部依赖」全列**（§16 Run 首屏门 KNOWN_RUN_GAP、卡 8be0e547 dual-model binding 加固〔用户已授权重启〕、M6b route-level 断连硬化、前端 echarts lazy-load〔用户拍板面〕等）。

## 活跃上下文
- **audit 基线（配方已钉死）**：canonical 稳定基线（**排除可变运行时 DB `goal_proof_ledger/`**）= **61 files / 20,339 lines / 26,209,663 bytes / manifest SHA-256 `7348e826…d9c9e9`** 全程字节不变（=交接基线·完整性未破）。⚠️ 旧记 `1c1788b0bbe2` 是**非规范配方**（行序不同）非漂移。**规范配方**：`find data/audit -type f -not -path '*/goal_proof_ledger/*' -print0 | sort -z | xargs -0 shasum -a 256 | shasum -a 256`。含 goal_proof_ledger 的全目录 manifest 可变、不作稳定基线。
- **测试基线（实跑·VaR/ES land 后）**：后端全量 **7019 passed / 14 skipped / 0 failed**（=6931 基线 + 88 risk-measures·隔离 DATA_ROOT·带 timeout·非并行叠跑·真汇总行·CI 29544874836 success）；validate_dev PASS。前端/benchmark 未受本切影响（纯后端 math）。
- **待用户复核（非阻塞）**：① 工作区 2 个 fib `.ipynb` 无记录删除在 `stash@{0}`（删/留待定）② data/audit spurious `goal_proof_ledger.sqlite`（删/留待定）。
- **⚠️ 工作区未提交（WIP·本 loop 断点·非漂移）**：真 ERC 切片全套（见上「IN-PROGRESS 组合 correctness」）——`_erc_solver.py`(新·solver) + `spine_binding.py`(新·命门) + `test_portfolio_erc_spine.py`(新·25 测) + M `optimizers.py`/`__init__.py`/`business_tools.py`/`tool_schema.py`/`test_agent_business_tools_a4.py`——**故意未 commit**：R2 全量绿 + codex floor R2 FLOOR-HOLD 前不 land（种坏门必抓 + 命门 + 四门纪律）。next fire 查 R2 两 log → 齐则整切一次 land（product 单 commit）+ D-MATH-P0A-ERC。设计+R1 floor 全文存 scratchpad（`erc_consolidated_design.md`/`erc_floor_out.md`/`erc_decision_draft.md`）。
- **工作模式**：实质性切片 duet（deep-opus ‖ codex gpt-5.6-sol）+ 跨厂商 codex floor 复审（approver≠creator·builder≠verifier）；codex 传 prompt 用 heredoc 文件避免 backtick 被 shell 解析；全量 pytest 隔离 DATA_ROOT + 带 timeout + 凭真汇总行判绿（非 exit code）；不 BLOCK 于前台长跑套件——后台跑 + 通知/tail。
