# v1.0.0-rc1 · QuantBT 全栈交付候选

发布日期 (UTC): 2026-05-29
分支: `fullstack`

## 一句话总结

24h sprint 完成 v0.9.5 → v1.0.0-rc1 全栈推进：
真 LLM streaming + PWA + Mainnet 7 项防御 + Tauri/Cloud/Stripe scaffold +
Lopez de Prado 学术 audit (4 项) + Binance USDM testnet 12 cell e2e + Glossary 30 词条。
**项目已推进到"只剩用户信息"才能完成的状态**。

## 自上一里程碑以来的工作

### v0.9.6 · binance/verify endpoint
- `POST /api/security/binance/verify`：用 keystore record 实际打 /fapi/v2/balance
- 返回 `is_correct_network` + USDT 余额，**真实验证用户测试盘 key**
- 用户测试: testnet key → USDT 5027.87 ✓

### v0.9.7 · 学术 audit 4 项 (Lopez de Prado patch1 §G.a)
1. **walk_forward_v2**: 每窗口候选集 + `selected_params_hash` + `detect_gridsearch_leak`
2. **factor_factory/audit**: `assert_shift_invariance` (后追加 future 不能改历史输出)
3. **portfolio/hrp_audit**: 协方差奇异性 fallback ladder
   - hrp → hrp_shrunk (Ledoit-Wolf 0.3) → risk_parity → equal_weight
4. **data_hash/dataset_hash**: `DatasetManifest` SHA-256 + `FactorBinding(factor_id, dataset_id, dataset_version)`

测试: `test_academic_audit_v2.py` 28 项 PASS

### v0.9.8 · 真 LLM SSE streaming
- `OpenAILLM.stream_chat()` 用 requests stream=True + iter_lines
- 前端 Mode2ChatPage 用 fetch + ReadableStream 消费 SSE
- curl -N 验证 token-by-token: `data: {"chunk": "**结论"}` ...

### v0.9.9 · PWA + 移动端
- `manifest.webmanifest` + service worker (cache-first 静态, network-first /api/*)
- 200 行 mobile media queries + 汉堡菜单 MobileDrawer
- 安装到桌面/手机 ✓

### v0.9.10 · Binance testnet 12 cell e2e
- 13 tests with `@pytest.mark.testnet`
- 6 order_type × 2 side: limit/market/stop_market/take_profit/stop_loss/trailing_stop_market
- 正确 trigger 方向 (修复 -2021 / -4183 错误)
- `scripts/run_testnet_matrix_e2e.py` CLI runner 写入 `trading_testnet_matrix`

### v0.9.11 · Glossary 30 词条
- 3 个完整 (sharpe_ratio / pbo / deflated_sharpe)
- 27 个 stub 占位 (strict_related 校验通过)
- GPT Pro 出真内容后只需 replace 文件

### v1.0.0-alpha · Mainnet 7 项防御
- `MainnetGuardsService` (26 tests PASS):
  - Per-user Fernet (PBKDF2 100k from `QUANTBT_MASTER_KEY`)
  - TOTP RFC 6238 (Google Authenticator 兼容)
  - IP 白名单 (exact + `1.2.3.*` wildcard)
  - 单日 operations + notional 上限
  - Per-order 密码二次确认
  - Append-only audit log
  - `assert_mainnet_allowed` 综合校验
- 6 个 endpoint: `/api/security/mainnet/{config,2fa/enable,2fa/verify,usage,audit_log,emergency_close_all}`

### v1.0.1-scaffold · Tauri 桌面
- `app/desktop/`: Tauri 2 + Rust 包 Mac/Win
- spawn Python backend (PyInstaller pack) + window CSP
- 用户拿到 Apple Developer ID + Win 代码签名证书后 `tauri build` 即可

### v1.0.2-scaffold · 阿里云部署
- `deploy/cloud/docker-compose.prod.yml`: caddy + backend + postgres + watchtower
- `deploy/cloud/Caddyfile`: 自动 HTTPS + HSTS + 安全 header
- `deploy/cloud/README.md`: 11 节阿里云轻量香港部署手册

### v1.0.3-scaffold · Stripe billing
- `BillingService` (11 tests): 3 plan + webhook event 处理
- `customer.subscription.{created,updated,deleted}` + `invoice.payment_failed`
- 用户在 Stripe Dashboard 建 price 后填 price_id 即可上线

### v1.0.4 · 前端 Mainnet Settings UI
- `/settings/security` 页面 (7 个面板)
- 2FA QR enroll + verify
- IP 白名单 textarea
- 单日额度 input + 今日 usage 实时显示
- Audit log table 最近 100 条
- 紧急平仓按钮 (需 TOTP 确认)
- Shell UserMenu 加 ⚙ 入口

### v1.0.5 · Stripe 前端接入
- PricingPage 接 `/api/billing/me` + `/upgrade_request`
- 当前 plan 高亮 + "下次续费" 日期
- 按年付 toggle
- 升级 → POST upgrade_request → 弹出 checkout_url

## 用户接手清单 (只剩这些)

按 `docs/strategy/_PROMPT_FOR_GPT_PRO_TESTNET.md` 和 `docs/user-quickstart-v0.9.md` 完成:

1. **Binance**: mainnet API key 创建 (no withdraw + IP 白名单)
2. **域名**: 注册 + 配 DNS 指向阿里云轻量服务器 (香港)
3. **阿里云**: 开机 + `deploy/cloud/deploy.sh` 一键起栈 (Caddy 自动签证书)
4. **Stripe**: 注册账号 + Dashboard 建 3 个 Product (Community/Learn/Live Pro) + monthly/annual price
   - 把 6 个 price_id 填到 `app/backend/app/billing/stripe_service.py` PLAN_INFO
   - webhook secret 填 `secrets.yaml`
5. **桌面包**: Apple Developer ID + Win EV 代码签名证书 → `tauri build`
6. **Glossary 27 stub**: 跑 GPT Pro 出真 L1-L4 内容 (按 `docs/glossary/_PROMPT_FOR_GPT_PRO.md` 6 批次)
7. **QUANTBT_MASTER_KEY**: 生产环境强 32 byte 随机 (cryptography Fernet.generate_key())

## 不在 v1.0 范围

- Copy-trade 公开排行榜 (法律风险，仅 follower self-keystore 工具 beta)
- 收益分成 / GMV 抽佣
- 真上线的 mainnet 实盘资金（用户自决）
- 移动端原生 App (PWA 已覆盖移动浏览器)

## 测试基线

- 后端 pytest: 588+ tests
- 前端 TSC: 0 errors
- 27 个 testnet matrix cell pass (1 个偶发 flaky on take_profit_sell)
- Glossary strict_related 校验: 30 terms, 0 invalid
