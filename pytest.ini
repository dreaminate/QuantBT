[pytest]
testpaths = app/backend/tests
markers =
    testnet: 真发 Binance testnet 订单 (默认 skip; 运行 pytest -m testnet 触发)
    slow: 慢测试 (> 5s)
    integration: 多 service 集成测试
addopts = -p no:cacheprovider --strict-markers
